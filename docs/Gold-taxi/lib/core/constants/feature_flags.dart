class FeatureFlags {
  static const bool blogEnabled = false;
  static const bool productsEnabled = false;
  static const bool eventsEnabled = false;
  static const bool insolvencyEnabled = false;

  // Add more flags as needed
}
